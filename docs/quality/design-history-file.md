# Design History File (DHF) — index (scaffold)

> **Scaffold.** ISO 13485 §7.3 requires a DHF: the compiled record of the design & development
> of the device. This is the *index* pointing at each record. Records marked **TODO** don't
> exist yet. To be maintained by QA/RA.

| Design record | Location | Status |
|---------------|----------|--------|
| Regulatory strategy & classification | `docs/regulatory-foundation.md` | started (B or C, unresolved as of 2026-08 — see §2.3) |
| Software safety classification (formal) | TODO | **open** — must be decided by risk analysis; 2026-08 re-examination against CDSCO/MD/GD/MDSW/01/2026 Table 2 found the prior B assumption unsafe to keep as-is, but landed on "B or C depending on one judgment call," not a flat C |
| User needs / intended use | `docs/requirements/intended-use-statement.md` | started — CDSCO §6.0 structure, open items flagged inline |
| Software requirements (SRS) | `docs/requirements/software-requirements.md` | started |
| Requirements traceability matrix | `docs/requirements/traceability-matrix.md` | started |
| Architecture description | code (`presentation/` → `domain/` → `data/`); formal doc TODO | partial |
| Detailed design | code + KDoc; formal doc TODO | partial |
| Risk management file | `docs/quality/risk-management-file.md` | started |
| Verification & validation records | TODO (see roadmap #4: tests + CI) | **open** |
| SOUP validation record (on-device ASR: sherpa-onnx, ONNX Runtime, Parakeet int8) | `docs/quality/soup-validation-record.md` | **PROPOSED, awaiting operator sign-off** (2026-09-02, PR 4b) - covers the five model-companion components that the generated SBOM cannot see |
| Usability engineering file (IEC 62366) | TODO | open |
| Cybersecurity / threat model | partial (SQLCipher, Keystore); doc TODO | partial |
| Release records | TODO | open |
| Change history | git commit history of this repository | available |

## Notes
- The clean layered architecture and pinned dependency versions give the DHF a real
  configuration baseline today (git history + `libs.versions.toml`).
- The biggest open DHF gaps are **V&V records** (blocker #4) and the **formal safety
  classification** (blocker #2) — both gate a credible submission.

## Change log

| Date | Change | Files affected | Rationale |
|---|---|---|---|
| 2026-09-02 | **On-device ASR reaches a user for the first time, and its SOUP gets a validation record.** `FeatureFlags.VOICE_FIELD_IMPACT_ENABLED` flipped `true` (PR 4b), making the voice confirmation gate reachable on one narrative field, `impactOnDailyActivities`. `VOICE_INPUT_ENABLED` stays `false`, so `chiefComplaint` (the field that reaches `/api/v1/evaluate`) and the audio attachment remain hidden. The flip landed only behind transmission evidence: reflection scan, decode, a measured zero byte delta across a full decode, `StrictMode`, plus a capture-writes-no-file check and an airplane-mode transmission witness run on the arm64 iQOO I2302 on the shipped ABI. New SOUP validation record covers all five components of the hand-maintained model companion; H-15's residual was rewritten from scratch because the clause every previous update rested on (not user-reachable) stopped being true; the intended-use statement was amended to say what the device now does. Two items stay open on purpose: an accented-speech evaluation of the shipped int8 artifact (a precondition of clinical deployment, not of this dev flip), and the absence of anyone reviewing the `dwellMs` rubber-stamp breadcrumbs. | `docs/quality/soup-validation-record.md` (new), `docs/quality/risk-management-file.md` (H-15), `docs/requirements/intended-use-statement.md` (section i), `config/FeatureFlags.kt`, `presentation/common/PermissionAction.kt`, `presentation/consultation/*` | A safety-relevant flag may not flip on a claim, only on evidence, and once it flips the controlled documents have to describe the device that now exists rather than the one that was dark. The SOUP most relevant to this release is precisely the part the SBOM generator cannot see, so it needed a record made by hand. |
| 2026-08-14 | **CI split into a per-flavor matrix + a tag-triggered signed release workflow.** `android-ci.yml`: matrix over `Dev`/`Staging`/`Prod`, each leg running `assemble{Flavor}Debug` then `test{Flavor}DebugUnitTest`; triggers on push to `master`/ABHA branches and PRs into `master` (was: push/PR on all branches, single `assembleDebug`/`testDebugUnitTest` leg — didn't exercise flavor-scoped BuildConfig differences at all, e.g. would have missed REQ-SEC-05/REQ-SEC-07 flavor bugs). New `android-release.yml`: fires only on `v*.*.*` tags, runs `testProdReleaseUnitTest` → `assembleProdRelease`, signs via `signingConfigs.release` (new in `app/build.gradle.kts`, reads `KEYSTORE_PATH`/`KEYSTORE_PASSWORD`/`KEY_ALIAS`/`KEY_PASSWORD` env vars, applied only to the `release` build type so debug/dev builds are unaffected by its absence), uploads the signed APK as a build artifact. **No prod signing keystore exists in this repo or was generated for this pass** — a signing key is a long-lived credential that must be created deliberately and backed up outside git (password manager/secrets vault), not conjured by an agent; the release workflow will fail at the decode step until `KEYSTORE_BASE64` + the three secrets are provisioned by a human in GitHub Secrets. `.gitignore` also gained `*.jks`/`*.keystore` — it had no keystore exclusion at all before this pass, a gap that only mattered once a real keystore exists to accidentally commit. | `.github/workflows/android-ci.yml`, `.github/workflows/android-release.yml` (new), `app/build.gradle.kts`, `.gitignore` | Task requirement: CI must actually cover all three environment flavors (the flag-off-by-default dev/staging/prod split introduced by REQ-SEC-05/06/07 is otherwise untested by CI), and a release path must exist for cutting a signed prod build from a version tag without committing signing secrets. |
| 2026-08-14 | **FLAG_SECURE flavor-gated, matching the cleartext-traffic pattern** — new flavor-scoped `SCREEN_SECURITY_ENABLED` BuildConfig field (`dev` = `false`, `staging`/`prod` = `true`), replacing the previous hardcoded-off `FeatureFlags.SCREEN_SECURITY_ENABLED` constant. `dev` staying off is intentional — investor/demo recordings need screenshots/screen-recording to work — and is not a regression; `staging`/`prod` enforce the flag on all patient-data routes as before. Per-route classification (`SECURED_ROUTE_TYPES`/`requiresScreenSecurity` in `Routes.kt`) and the window-wide apply site in `AppNavHost` are unchanged, only the on/off source moved from a constant to `BuildConfig`. `RoutesSecurityTest` rewritten to assert against the running flavor's own `BuildConfig.SCREEN_SECURITY_ENABLED` value (true branch on staging/prod, false branch on dev) instead of assuming the flag is always on, so it's meaningful — not just passing by accident — under all three flavors. | `app/build.gradle.kts`, `config/FeatureFlags.kt`, `app/src/test/.../RoutesSecurityTest.kt` | `RoutesSecurityTest` was failing because FLAG_SECURE was globally hardcoded off for demo recording — a real privacy control silently disabled everywhere, not just in dev. Flavor-gating it (same pattern as REQ-SEC-05 cleartext traffic) keeps the demo workflow working in dev while restoring real screen security in staging/prod. |
| 2026-08-14 | **Environment separation doc corrections** — `agent_docs/CLAUDE.md` Networking section corrected to say base URLs come from `BuildConfig` fields (flavor-scoped), not a hardcoded IP; new `## Environments` section with per-flavor table. `agent_docs/spec.md` package structure corrected to the real current `presentation/` module list; DB version references confirmed at v12/`MIGRATION_12_13`. `docs/data-retention.md` schema version corrected 11→12. compileSdk/targetSdk remain at **37** — no SDK version change was made this session. | `agent_docs/CLAUDE.md`, `agent_docs/spec.md`, `docs/data-retention.md` | Prior session's doc pass missed propagating the new flavor-scoped BuildConfig sourcing into the Networking section and left package structure/screen flow stale against the real module list. |
| 2026-08-14 | **Gradle product flavors: `dev`/`staging`/`prod`** — new `flavorDimension "environment"`, flavor-scoped `KERNEL_BASE_URL`/`BACKEND_BASE_URL`/`ABHA_BACKEND_BASE_URL`/`ENVIRONMENT` BuildConfig fields. `dev` = HTTP + LAN IPs + cleartext (moved into a new `src/dev/AndroidManifest.xml` override, removed from the main manifest); `staging`/`prod` = HTTPS placeholder URLs, cleartext blocked by platform default. `dev`/`staging` get `applicationIdSuffix` so all three can coexist on one device. | `app/build.gradle.kts`, `app/src/main/AndroidManifest.xml`, `app/src/dev/AndroidManifest.xml` (new) | Prep for the real backend (`backend/core/`, `backend/abdm-adapter/`) and ABDM sandbox/production split — staging and prod must never allow cleartext HTTP, only the local dev loop should. |
| 2026-08-13 | **CDSCO classification re-examined against newer guidance; found B or C, unresolved (not a flat C — corrected same-day after an internal table-lookup error).** Read the newer CDSCO Guidance Document on Medical Device Software (Doc No. CDSCO/MD/GD/MDSW/01/2026, supersedes the Oct-2025 draft). Table 2 columns are Treatment/diagnosis\|Drive\|Inform, rows Critical/Serious/Non-serious = (D,C,B)/(C,B,A)/(B,A,A). Base case for this app (Serious situation × Drive-clinical-management output) is **B**, matching the prior assumption — first pass wrongly read this cell as C. The actual open question: the guidance's non-clinical-user note may escalate Serious→Critical (since the doctor's AGREE/MODIFY/REJECT review is asynchronous, not co-located with the field worker at the point of AI output), which would make it Critical×Drive=**C**. That escalation is arguable, not settled — documented as the single swing factor rather than asserting either class. Also added: licensing-pathway note (Class C → Central Licensing Authority, not State, if C is confirmed), new AI-specific QMS obligations (Algorithm Change Protocol, drift monitoring, IS/ISO/IEC 42001+23894) not previously tracked, `docs/requirements/intended-use-statement.md` (CDSCO §6.0 structure), and `docs/requirements/abha-real-api-mapping.md` (planning-only map of the real ABDM sandbox API surface in `abha api docs/` against the existing mocked ABHA flow — no code changes). | `regulatory-foundation.md`, `qms-overview.md`, `risk-management-file.md` (H-02 note), `intended-use-statement.md` (new), `abha-real-api-mapping.md` (new) | User requested a full documentation audit against the newer CDSCO guidance and the real ABHA API docs before any further ABHA integration work begins; classification is the load-bearing decision everything else in the regulatory roadmap depends on. User caught the table-lookup error on review — corrected same session rather than left standing. |
| 2026-07-20 | **UI terminology: "Chief complaint" → "Main concern"** across all presentation-layer screens and report narrative strings. Internal code/DB field names (`chiefComplaint`) intentionally unchanged — no schema migration required. | `ConsultationScreen`, `CompounderScreen`, `DoctorListScreen`, `ConsultationChainScreen`, `PatientSummaryScreen`, `ReportFormatter`, `report-field-mapping.md` | Patient-facing language should be warm and accessible. "Chief complaint" is medical jargon; "Main concern" is plain English and more humane. |
| 2026-07-20 | **Investor demo auto-fill** — new `data/mock/DemoPatientProfile.kt` holds a clinically consistent patient persona (Priya Sharma, 34F, Shivpuri MP). Each screen gains a `fillDemoData()` action wired to a "👤 Fill demo patient data" button that pre-populates every field in one tap. Data is isolated to the demo path and never touches production storage. | `DemoPatientProfile.kt` (new), `RegisterViewModel`, `RegisterScreen`, `MedicalBackgroundViewModel`, `MedicalBackgroundScreen`, `CompounderViewModel`, `CompounderScreen`, `ConsultationViewModel`, `ConsultationScreen` | Eliminate manual data entry during investor presentations; all fields populated with realistic rural-India clinical data. |
| 2026-07 | **`/api/v1/evaluate` real-inference integration** — new data/domain layer (`EvaluateRequestDto`/`EvaluateReportDto` tree, `ClinicalApiService`, `EvaluateKernelSource`/`RetrofitEvaluateSource`, `EvaluateReportOutput`/`EvaluateReportRepository`, `GenerateEvaluateReportUseCase`), fired alongside the existing `/v1/assess` call. No mock fallback on this leg by design. Also fixed two real bugs found while integrating: (1) backend's import order (`xgboost`/`shap` before the torch-dependent RAG chain) corrupted OpenMP DLL state on Windows — fixed in `SaMDClassifier/app.py`; (2) app sent `sex: "Male"/"Female"` but backend checked `sex.upper() == "M"` exactly, silently mis-coding every male patient — fixed in `RetrofitEvaluateSource`/`RetrofitKernelSource` (`.take(1).uppercase()`). | `data/remote/dto/Evaluate*.kt`, `data/remote/api/ClinicalApiService.kt`, `data/remote/RetrofitEvaluateSource.kt`, `domain/kernel/EvaluateKernelSource.kt`, `domain/model/EvaluateReportOutput.kt`, `domain/repository/EvaluateReportRepository.kt`, `data/repository/EvaluateReportRepositoryImpl.kt`, `domain/usecase/GenerateEvaluateReportUseCase.kt`, MIGRATION_8_9, `SendingViewModel`, `di/NetworkModule.kt` (LAN IP updated to `10.16.4.182`) | The old `/v1/assess` contract had no treatment-recommendation data (drug/dosage/referral); `/api/v1/evaluate` on the same backend does. |
| 2026-07 | **5 clinically coherent demo personas** replace the single "Priya Sharma" persona — each picked from the classifier's actual training vocabulary/vitals-grading thresholds so real inference lines up with the presenting picture (previously mismatched, e.g. a fever persona coming back a hypertension diagnosis). Persona picker (dropdown, reusing `DropdownField`) added to `RegisterScreen`. | `data/mock/DemoPatientProfile.kt` (rewritten: `Persona` data class + `PERSONAS` list + `select(index)`), `RegisterScreen.kt` | Investor demo credibility — mock data that produces mismatched AI output undermines the whole point of showing real inference. |
| 2026-07 | **AI Clinical Evaluation report/prescription section** — `ReportCanvasRenderer.evaluateBlock`, drawn in fixed order (vitals triage → top diagnostic only → recommended treatment+brand → overall urgency → inference time, small line at bottom). Bolds diagnosis/drug/brand; bold red for urgent/human-review/pediatric-referral flags — "what a doctor's eye needs to land on first." The old "Kernel AI Assessment" block (`kernelBlock`, `/v1/assess`-sourced, could show mock-fallback data) was removed from the canvas entirely — superseded by this section, which has no mock path to begin with. | `ReportCanvasRenderer.kt` (`kernelBlock` removed, `evaluateBlock` added, `boldBodyPaint`/`urgentPaint` added), `report-field-mapping.md` | Investor-facing report should show the real AI output prominently, not a mock-capable legacy block; doctors scan for critical findings first. |
| 2026-07 | **India-brand lookup via Gemini** — `BrandLookupSource`/`GeminiBrandLookupSource` asks Gemini for the top-selling India-manufactured brand **and manufacturer** for the AI-recommended drug (`gemini-2.5-flash`, `thinkingBudget: 0`). Two real bugs found and fixed during integration: (1) `gemini-2.0-flash` had zero quota on the provided key — switched model to `gemini-2.5-flash`; (2) Gemini's "thinking" mode added ~5s latency, sitting right past the original 6s OkHttp timeout — silent timeouts caused "Not available" even with a valid key; disabled thinking (0.7s typical) and raised timeouts to 10s/12s for margin. Also fixed a Gradle config-cache bug: `local.properties` reads weren't tracked as configuration-cache inputs, so editing the API key alone didn't invalidate the cached `BuildConfig` value. | `data/remote/GeminiBrandLookupSource.kt`, `data/remote/api/GeminiApiService.kt`, `data/remote/dto/GeminiDto.kt`, `di/GeminiNetworkModule.kt`, `domain/kernel/BrandLookupSource.kt`, `domain/model/EvaluateReportOutput.kt` (`IndianBrandSuggestion`), `app/build.gradle.kts` (`providers.fileContents` for `local.properties`) | Prescription needs a real, purchasable brand name, not just the generic drug name; company name requested explicitly so the reviewer can judge manufacturer credibility (e.g. Cipla, Sun Pharma). |
| 2026-07 | **Doctor-review relocated + made real** — the AGREE/MODIFY/REJECT physician decision was first (wrongly) added to the worker-facing AI Assessment screen, then moved to its correct location: `PatientSummaryScreen`'s doctor-response flow (button renamed from "Check for doctor's response (mock)" to "Review AI diagnosis (doctor)"). New `SubmitDoctorDecisionUseCase` replaces the random-roll `MockDoctorPrescriptionInbox` call site (that class + `ReceiveDoctorPrescriptionUseCase` left in place, unused, see traceability-matrix orphaned-but-passing note) — AGREE prescribes exactly the kernel's drug/dosage/brand; MODIFY/REJECT let the reviewer type their own, with a one-tap Gemini brand re-lookup. Hardcoded Paracetamol mock fallback removed. | `domain/usecase/SubmitDoctorDecisionUseCase.kt` (new), `presentation/patientsummary/PatientSummaryViewModel.kt`+`Screen.kt` (rewritten), `presentation/kernelassessment/*` (reverted to pre-review state) | "Doctor has the power to refine the model" needed to live where the worker checks the doctor's response, not on the pre-diagnosis AI Assessment screen — and needed to be a real choice, not a weighted random roll, to mean anything for an investor demo. |
| 2026-07 | **Diagnosis-refinement dataset-safety fix** — checked `SaMDClassifier/train_model.py` and `train_symptom_classifier.py` directly: neither reads drug/brand/company columns; Classifier A trains on `age,sex_encoded,systolic_bp,diastolic_bp,bmi,heart_rate,spo2,glucose`→`tier`, Classifier B on `symptom_string`→`icd_candidate`. Found `DiagnosisFeedback.physicianFinalDiagnosis` was hardcoded null always — MODIFY and AGREE produced identical training-relevant records, defeating the point of MODIFY. Fixed: new `TRAINED_ICD_CANDIDATES` (the 18 classes `symptom_model.json` actually trained on) dropdown, shown only on MODIFY, validated in `SubmitDoctorDecisionUseCase` to be one of the 18 or dropped to null; new `clinicalNote` free-text field, explicitly separate, never reimportable. Drug/dosage/brand confirmed to flow only into `Prescription`/`MedicationLine`, never `DiagnosisFeedback`. | `domain/model/TrainedIcdCandidate.kt` (new), `domain/model/DiagnosisFeedback.kt` (`clinicalNote` added), `data/local/entity/DiagnosisFeedbackEntity.kt` (MIGRATION_10_11), `domain/usecase/SubmitDoctorDecisionUseCase.kt`, `presentation/patientsummary/*` | User's explicit ask: verify what data actually feeds model retraining before shipping a feedback UI that could silently corrupt a future training-dataset reimport with irrelevant (drug/brand) or unreimportable (free-text/out-of-class) data. |
